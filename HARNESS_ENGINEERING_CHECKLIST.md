# Harness Engineering Checklist

本工程默认采用 Harness Engineering 范式，重点要求如下：

- 先明确边界：页面、接口、调用方、失败路径、兼容影响。
- 先补闭环：功能、加载态、错误态、空态、移动端表现一起交付。
- 先保契约：接口变更必须同步检查 web、backend、admin 之间的契约影响。
- 先保可观测：关键交互要能通过日志、报错或埋点快速定位问题。
- 先做验证：至少完成最小可行验证，如 type check、build、关键交互手测。
- 先看回滚：避免把高风险接口、状态和 UI 语义改动一次性耦合上线。
